# README for TCCSNet Reimplementation

Reimplementation of code from the paper:
"Convolutional Nerual Network With Multihead Attention for Human Activity Recognition" by Tan-Hsu Tan *et al.* [^Tan-MHA-HAR]

## Repository Contents

The model architecture for the PyTorch model is defined in `CNN_MHA_Classifier.py`.

Training is managed by `Train_CNNMHA.py` and evaluation results are generated by `Eval_MHA.py`, with details on how to run them listed below.

Example models for each dataset are provided in the `Models/` directory.


## TO TRAIN MODELS



To train a model, use the following prompt in the command line.

`python Train_CNNMHA.py [FolderName] [Cut] [Stride] [DATABASE_Flag] [FoldSegFlag] [FoldNumber] [TotalFolds]`

- Train_TallyNet.py
	* Python Script to train TallyNet model

- `FolderName`
	* Output folder location to store all files generated with the specific fold and model currently training

- `Cut`
	* Total duration of the window used as an input to the model
	* Units are in seconds

- `Stride`
	* Step which the dataset will slide the window forward through the motion data to generate the next example
	* Units are in Seconds

- `DATABASE_Flag` 
	* Flag used to select which dataset the model is working with
		- 1 = Dnd OREBA One-Handed
		- 2 = Dnd OREBA Two-Handed
		- 3 = Dnd Clemson
		- 4 = Dom OREBA One-Handed
		- 5 = Dom Clemson

- `--fold-seg [FoldSegFlag]`
	* Bool determining whether the k-fold validation is contiguous blocks or striped through all examples
		- 0 = Block Segmentation
		- 1 = Striped Segmentation (Recommended Value)

- `FoldNumber`
	* Number of the fold used in k-fold validation (ranging from 1 to `TotalFolds`) used for training and testing split

- `TotalFolds`
	* Total number of folds used in k-fold validation



- Example
	* `python Train_CNNMHA.py ExampleModel/models/fold1 5.12 0.3 2 1 1 5`






## TO EVALUATE MODELS

To evaluate a model, run the following command in the command line.


`python Eval_MHA.py [Folder_Name] [DatabaseFlag] [FoldNumber] [TotalFolds] [OPTIONAL_ARGS]`

- Eval_MHA.py
	* Python Script used for evaluation

- `FolderName`
	* Name of the folder which contains the model file
	
- `DATABASE_Flag`
	* Flag used to select which dataset the model is working with
		- 1 = Dnd OREBA One-Handed
		- 2 = Dnd OREBA Two-Handed
		- 3 = Dnd Clemson
		- 4 = Dom OREBA One-Handed
		- 5 = Dom Clemson

- `FoldNumber`
	* Number of the fold used in k-fold validation (ranging from 1 to `TotalFolds`) used for training and testing split

- `TotalFolds`
	* Total number of folds used in k-fold validation

- `OPTIONAL_ARGS`
    * `--stride [float]`
		- Default Value = 1.0 sec
		- Step which the dataset will slide the window forward through the motion data to generate the next example
		- Units are in Seconds
    * `--resamp [int]`
		- Resampling Rate in Hertz to which all datasets will be interpolated to match.
		- Lower Sampling rate decreases memory and computation, but also model capabilities on harder datasets.
    * `--fold-seg [int]`
		- Default Value = 1
		- Bool determining whether the k-fold validation is contiguous blocks or striped through all examples
			* 0 = Block Segmentation
			* 1 = Striped Segmentation
    * `--save-preds [string]`
		- Filepath to save all model predictions to a `.csv` file
    * `--save-dets [string]`
		- Filepath to save all model detections to a `.csv` file
    * `--save-gts [string]`
		- Filepath to save all model ground truths used in eval to a `.csv` file
    * `--trigger [float]`
		- Value of the prediction to trigger a detection 
    * `--add-eval [int]`
		- Default Value = [1]
		- Evaluation to run on the generated predictions,
		  dictating how detections are paired to ground truth events
			* 1 = Point to Point Evaluation, 
				taken from Dong *et al.*[^DongHeurPaper]
			* 2 = Point to Window with Tolerance, 
				modified from Kyritsis *et al.*[^KyritsisMicroMotion]
	* `--tol [float]`
		* Default = 0.0
		- Tolerance used in Point to Window Evaluation, 
		  added to both sides of the window duration 
		  to match a possible detection within 



- Unused `Cut`
	* Eval always uses 5.12 seconds, since this is the value used in the original paper
	* Units are in seconds


- Full Example
	* `python Eval_MHA.py ExampleModel/models/fold1_best.pt 2 1 5 --stride 5.12 --trigger 0.65 --add-eval 1 >> Example_THO_Results.txt`


## References

[Tan-MHA-HAR]:T.-H. Tan, Y.-L. Chang, J.-R. Wu, Y.-F. Chen, and M. Alkhaleefah,
“Convolutional neural network with multihead attention for human
activity recognition,” IEEE Internet of Things Journal, vol. 11, no. 2,
pp. 3032–3043, 2024.

[^DongHeurPaper]: Y. Dong, A. Hoover, J. Scisco, and E. Muth, 
“A new method for measuring meal intake in humans
via automated wrist motion tracking,”
Applied psychophysiology and biofeedback, vol. 37, pp. 205–215, 2012


[^KyritsisMicroMotion]: K. Kyritsis, C. Diou, and A. Delopoulos,
“Modeling wrist micromovements to measure in-meal eating behavior 
from inertial sensor data,”
IEEE journal of biomedical and health informatics,
vol. 23, no. 6, pp. 2325–2334, 2019