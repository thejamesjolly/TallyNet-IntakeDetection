# README FOR TallyNet/Models

This Folder contains a set of example pre-trained models (one for each dataset variation and window size of TallyNet).

Dataset is named in the start of each directory. 
"WS##" refers to the window size in seconds of the input data,
where "##" is replaced with the numeric value.